import { SlottableViewProps, ViewRef } from '@rn-primitives/types';
import * as React from 'react';
import { ViewStyle, View } from 'react-native';

type RootProps = Omit<SlottableViewProps, 'style'> & {
    ratio?: number;
    style?: ViewStyle;
};
type RootRef = ViewRef;
declare const Root: React.ForwardRefExoticComponent<Omit<SlottableViewProps, "style"> & {
    ratio?: number;
    style?: ViewStyle;
} & React.RefAttributes<View>>;

export { Root, type RootProps, type RootRef };
